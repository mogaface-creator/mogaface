/**
 * Versions the treatment-opportunity rules and mapping logic in
 * lib/treatment-opportunities/ — independent of the observation engine,
 * facial-analysis methodology and assessment versions it reads from. Stored
 * on every generated TreatmentOpportunity as `methodologyVersion`.
 */
export const TREATMENT_OPPORTUNITY_ENGINE_VERSION = "0.1.0";
